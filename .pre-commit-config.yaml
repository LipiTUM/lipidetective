repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.0
    hooks:
      - id: ruff          # lint with auto-fix
        args: [--fix]
      - id: ruff-format   # format

  - repo: local
    hooks:
      - id: mypy
        name: mypy
        entry: uv run mypy src/ --check-untyped-defs
        language: system
        types: [python]
        pass_filenames: false
